{
  "name": "ollin-tui",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "XDG_CACHE_HOME=provision/.cache/xdg PLAYWRIGHT_BROWSERS_PATH=provision/.cache/playwright turbo run build --cache-dir provision/.cache/turbo --log-file provision/.cache/turbo/logs/build.json",
    "dev": "make dev",
    "format": "make format",
    "format-all": "make format-all",
    "test": "XDG_CACHE_HOME=provision/.cache/xdg PLAYWRIGHT_BROWSERS_PATH=provision/.cache/playwright turbo run test --cache-dir provision/.cache/turbo --log-file provision/.cache/turbo/logs/test.json",
    "typecheck": "XDG_CACHE_HOME=provision/.cache/xdg PLAYWRIGHT_BROWSERS_PATH=provision/.cache/playwright turbo run typecheck --cache-dir provision/.cache/turbo --log-file provision/.cache/turbo/logs/typecheck.json"
  },
  "devDependencies": {
    "@ollin/artefacts": "workspace:*",
    "@ollin/tsconfig": "workspace:*",
    "@types/node": "^25.6.2",
    "turbo": "^2.9.12",
    "typescript": "^6.0.3",
    "vitest": "^4.1.5"
  },
  "packageManager": "pnpm@11.1.1",
  "pnpm": {
    "onlyBuiltDependencies": [
      "@opentui/core",
      "esbuild"
    ]
  }
}
